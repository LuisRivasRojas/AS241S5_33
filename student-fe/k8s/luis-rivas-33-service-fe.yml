apiVersion: v1
kind: Service
metadata:
  name: fe-service
  namespace: luis-rivas-fe
spec:
  type: NodePort
  selector:
    app: student-fe
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30091
