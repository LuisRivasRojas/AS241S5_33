server:
  port: ${APP_PORT:8090}

spring:
  application:
    name: student-luis-rivas

  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-super-mode-ahbe7kpv.c-3.us-east-1.aws.neon.tech/studentdb?sslmode=require}
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_4LXUo5scMpKH}
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 5
      minimum-idle: 1
      idle-timeout: 300000
      max-lifetime: 1200000

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.luisrivas: INFO
    org.hibernate.SQL: DEBUG
